# ============================================================================
# ModelServe — Feast Feature Store Configuration
# ============================================================================

project: modelserve
provider: local
registry: data/feast_registry.db

online_store:
  type: redis
  connection_string: ${REDIS_HOST}:6379

offline_store:
  type: file
