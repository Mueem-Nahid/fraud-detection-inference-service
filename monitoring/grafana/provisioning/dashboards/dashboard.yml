apiVersion: 1

providers:
  - name: ModelServe
    orgId: 1
    folder: ModelServe
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
