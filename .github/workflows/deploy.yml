name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  AWS_REGION: ${{ vars.AWS_REGION || 'ap-southeast-1' }}
  AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
  ECR_REGISTRY: ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ vars.AWS_REGION || 'ap-southeast-1' }}.amazonaws.com
  IMAGE_NAME: ${{ vars.ECR_IMAGE_NAME || 'modelserve' }}

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Cache pip dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements*.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov

      - name: Run pytest
        run: |
          pytest app/tests/ -v --tb=short

  build-and-push:
    name: Build and Push
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.ECR_REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest
            type=sha,prefix=,format=long

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.ECR_REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.ECR_REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Configure SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/ec2_key
          chmod 600 ~/.ssh/ec2_key
          echo "Host ec2
            HostName ${{ secrets.EC2_HOST }}
            User ${{ secrets.EC2_USERNAME }}
            IdentityFile ~/.ssh/ec2_key
            StrictHostKeyChecking accept-new" > ~/.ssh/config

      - name: Copy deployment configuration
        run: |
          ssh ec2 "mkdir -p /opt/modelserve"
          scp docker-compose.yml ec2:/opt/modelserve/docker-compose.yml
          scp -r monitoring ec2:/opt/modelserve/monitoring
          scp -r feast_repo ec2:/opt/modelserve/feast_repo
          scp -r training ec2:/opt/modelserve/training
          ssh ec2 "printf '%s\n' \
            'API_IMAGE=${{ env.ECR_REGISTRY }}/${{ env.IMAGE_NAME }}:latest' \
            'MODEL_NAME=fraud_detection_model' \
            'MODEL_STAGE=Production' \
            'MLFLOW_TRACKING_URI=http://mlflow:5000' \
            'FEAST_REPO_PATH=/app/feast_repo' \
            'REDIS_HOST=redis' \
            'REDIS_PORT=6379' \
            'GF_SECURITY_ADMIN_USER=admin' \
            'GF_SECURITY_ADMIN_PASSWORD=${{ secrets.GRAFANA_ADMIN_PASSWORD || 'admin123' }}' \
            'GF_USERS_ALLOW_SIGN_UP=false' \
            > /opt/modelserve/.env.deploy"

      - name: Pull Docker image on EC2
        run: |
          ssh ec2 << 'EOF'
            set -e
            aws ecr get-login-password --region ${{ env.AWS_REGION }} | docker login --username AWS --password-stdin ${{ env.ECR_REGISTRY }}
            docker pull ${{ env.ECR_REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            docker tag ${{ env.ECR_REGISTRY }}/${{ env.IMAGE_NAME }}:latest ${{ env.IMAGE_NAME }}:latest
          EOF

      - name: Restart service
        run: |
          ssh ec2 << 'EOF'
            set -e
            cd /opt/modelserve
            docker-compose --env-file .env.deploy down || true
            docker-compose --env-file .env.deploy up -d --remove-orphans
            docker image prune -f
          EOF

      - name: Wait for service to be healthy
        run: |
          sleep 10
          ssh ec2 << 'EOF'
            for i in {1..30}; do
              if curl -sf http://localhost:8000/health > /dev/null 2>&1; then
                echo "Health check passed!"
                exit 0
              fi
              echo "Waiting for service... attempt $i/30"
              sleep 2
            done
            echo "Health check failed!"
            curl -v http://localhost:8000/health
            exit 1
          EOF

      - name: Display deployment info
        run: |
          echo "Deployment completed successfully!"
          echo "Image: ${{ env.ECR_REGISTRY }}/${{ env.IMAGE_NAME }}:latest"
          echo "Commit: ${{ github.sha }}"
